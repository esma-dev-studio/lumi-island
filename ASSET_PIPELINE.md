# ASSET_PIPELINE.md — 3Dアセット制作パイプライン

この開発機では **Blender・ImageGenが利用不可**(確認済み: 2026-07-29)。
そのため優先順位2「利用可能な3D生成ツール」として、**自作のプロシージャルGLB生成器(Node + @gltf-transform/core)** をパイプラインの中心にする。
基本図形の直結ではなく、**断面プロファイル+ロフト/ラティス変形+スムーズ法線+スキニング** で有機的な形状を生成する。

## 全体フロー

```
1. コンセプト定義   … CHARACTER_SPEC.md に文章+パラメータで確定
                      (ImageGen不可のため、画像の代わりに数値仕様+Showcase実物で反復)
                      プロンプト相当の設計記述は .prompts/characters/ に保存
2. 生成器で形状生成 … tools/chargen/ (Node ESM)
                      - 断面ロフト(頭・胴・腕・脚を曲線プロファイルから生成)
                      - 種族パラメータ(耳・マズル・角・尻尾・体格)
                      - 服を別シェルとして生成(裾・袖・襟の段差)
3. リトポ相当      … 生成時に頂点数を制御(セグメント数パラメータ)。目標4k-9k tris
4. UV             … パーツごとに矩形アトラス割当(自動)。512pxテクスチャ
5. テクスチャ      … node-canvasは使わず、生成器内で数値的にピクセルを描画しPNG出力
                      (パレット+目・口・模様。プログラム描画)
6. リギング       … 共通26ボーン。ウェイトは距離+セグメント帰属から計算し正規化
7. アニメーション  … キーフレームをコードで記述(quaternion+位置)。9種+種族固有
8. GLB出力        … @gltf-transform/core で .glb を書き出し
9. 組込み         … public/assets/characters/ へ。src/data/characters.ts に登録
10. 検証          … Character Showcase Scene でスクショ→自己レビュー→パラメータ調整
11. 最適化        … gltf-transform で dedup/prune/quantize(必要時)
```

## 環境・建物・家具

- 環境(地形・木・岩・草)と建物は **Babylon.js内でプロシージャル生成**(VertexData直接構築)。
  実行時生成にすることでファイル容量ゼロ・バリエーション無限(シード固定で決定的)。
- 家具10種は tools/furngen/ でGLB化するか、環境と同じ実行時生成のどちらか軽い方を採用
  (小物はドローコール削減のためマージ)。
- すべて ART_DIRECTION.md の「ゆらぎ・面取り・手作り感」ルールに従う。

## 音

- WebAudio APIによる実行時合成(オシレータ+ノイズ+エンベロープ)。外部音源ファイルなし。
- 種別: 足音(草/土/砂)、オノ、ツルハシ、カマ、採取取得、水音/釣り、クラフト完成、UI、依頼達成、昼環境音(鳥・波)、夜環境音(虫・風)。

## ライセンス管理

- 外部アセットを使った時点で ATTRIBUTIONS.md に記録(名前/作者/配布元/ライセンス/改変)。
- 現時点の方針: **すべて自作生成**(コード・テクスチャ・音)。フォントはGoogle Fonts(SIL OFL)を使用予定→記録する。

## 再生成コマンド

```
npm run gen:characters   # 全キャラGLBを再生成
npm run gen:furniture    # 家具GLBを再生成(採用時)
```
生成はシード固定で決定的(同じ入力→同じGLB)。
