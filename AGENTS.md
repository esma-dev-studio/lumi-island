# AGENTS.md — 継続ルール(このプロジェクトで常に守ること)

## 実装の基本
1. **実装後は必ず起動して確認する。** コードを書いただけで完了扱いにしない。devサーバー起動→ヘッドレスEdgeで実際に描画・操作→スクショで確認。
2. **問題を報告するだけでなく修正する。** 自己レビューで見つけた指摘は同じ作業内で直す。
3. **完成版にプレースホルダーを残さない。** 仮テキスト・仮画像・TODO表示・ダミーボタンは出荷画面に出さない。
4. **基本図形キャラクターを完成版に使わない。** 球・円柱・カプセル・立方体の組み合わせ人物、棒人間、リグなし静止モデルは禁止。基本図形は当たり判定・デバッグ表示・開発中の一時代替のみ。
5. **未実装ボタンを表示しない。** 機能がないUIは置かない(グレーアウトで「未実装」も不可)。
6. **コンソールエラーを残さない。** 検証のたびに console を確認し、error 0 を維持。警告も原因を把握する。
7. **変更後に回帰テストを行う。** `npm run test`(Vitest)+ E2Eスモーク(移動→採取→クラフト→セーブ→復元)を通してからコミット。
8. **大規模な1ファイル実装を避ける。** 1ファイル約400行を超えたら分割を検討。システムごとに src/systems/ へ。
9. **ゲームロジックと表示を分離する。** systems はBabylon/DOMに依存しない純ロジック(テスト可能)。表示は scenes/ ui/ characters/ が担当し、イベントで接続。
10. **外部アセットのライセンスを記録する。** 使用したら即 ATTRIBUTIONS.md に追記(名前・作者・配布元・ライセンス・改変)。
11. **スコープ外の大型機能を勝手に追加しない。** PLAN.md 10章のリストを追加しない。

## この開発機の固有ルール
- `vite build` は 0xC0000409 で落ちることがある(環境起因)。ローカルは `vite dev` で完結。ビルド必須の場面はGitHub Actionsで行う。
- 画面検証は **puppeteer-core / Playwright + Edge**(`C:/Program Files (x86)/Microsoft/Edge/Application/msedge.exe`)。WebGLを実GPUで動かすときは `--use-angle=d3d11 --enable-gpu`。アプリ内プレビューペインは非表示タブでrAFが止まるため検証に使わない。
- 色コード・文言を書いた直後に同形異文字(キリル文字のe/a等)の機械チェック(`npm run check:chars`)を行う(過去に実害2回)。
- コミットは noreply メール(280012992+esma-dev-studio@users.noreply.github.com)。
- 着手前に `90_Private/ゲーム開発の教訓.md` の教訓に従う。新しい教訓は完了報告で追記提案。

## 品質ゲート
- キャラクターは Character Showcase Scene のスクショ自己レビュー(CHARACTER_SPEC.md のチェックリスト)に合格するまで、ゲーム本体の大量実装へ進まない。
- 各マイルストーン後に PLAN.md 8章の検証ループ(起動→操作→スクショ→コンソール→修正→回帰)を必ず実施。

## 進行管理
- マイルストーン完了ごとに PLAN.md のチェックボックスを更新し、コミットする(セッションが切れても状態が分かるように)。
- スクショは `.logs/screenshots/` に「対象_状態.png」で保存(例: showcase_night.png)。
